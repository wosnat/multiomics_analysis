# Evaluation milestone — notebook

Synthesis, not new computation. The deliverable is the finalized `paper.md` (at the
analysis root); the analysis milestone's outputs are trusted evidence.

## Co-define (agreed 2026-07-12)

- Finalize `paper.md` as a **negative-result finding** (researcher chose framing "a"),
  method lessons folded into the Discussion.
- Assess against the framing; harvest caveats; state what would move the question.
- Interpretation-only critic before sign-off, then the evaluation commit closes the arc.

## Do — what was written

Filled `paper.md` Methods (finalized), Results (the negative, contrast by contrast),
Discussion (why the signal may be absent; four method lessons; next steps; bottom line).
No new computation — all numbers cite the analysis milestone outputs.

## Critical review (interpretation only) — 1 Concern, no Blockers

Fresh-context critic over `paper.md` (analysis outputs = trusted evidence). Verified
every headline claim against the scored CSVs and prior reviews; references used
correctly; benzoate (expected-negative aromatic) and Fe-citrate (iron) framings correct;
causal language hedged throughout.

- **Concern (FIXED)** — the Results summary said the expected-negatives were "the only
  modules to clear the significance bar **anywhere**," which contradicts the paper's own
  temporal q<0.10 hits (L-lactate, peptide/nickel, carbohydrate). Reworded: no module met
  the pre-registered criteria to **name** a source; in the **presence** contrasts only the
  two expected-negatives cleared q<0.10; the temporal hits cleared the bar but cannot name
  a source (confounded, corroboration-only) — the calibration the Discussion already used.
- **Note (FIXED)** — "a validated method" scoped to "a method validated on the primary
  contrast" (the temporal contrast's ribosomal-neutrality control failed).
- Method lessons + "exudate metabolomics is the decisive next measurement" — critic
  confirmed all four lessons are supported and the KG holds zero coculture exudate
  metabolomics.

## Decide-gate checklist (evaluation)

- [x] Paper finalized as a bounded negative-result finding
- [x] Every conclusion traced to an analysis-milestone number (no new computation)
- [x] Caveats harvested (single-gene modules, orphan SBPs, significant-only sparsity,
      temporal growth-state confound); next steps stated (exudate metabolomics)
- [x] Interpretation critic run; 1 Concern + 1 Note fixed; no Blockers
- [ ] **Researcher sign-off → evaluation commit (closes the analysis)**
